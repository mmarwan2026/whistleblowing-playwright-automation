{
  "name": "whistleblowing-playwright",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:chromium": "playwright test --project=chromium",
    "test:smoke": "playwright test --grep @smoke",
    "test:regression": "playwright test --grep @regression",
    "test:e2e": "playwright test --grep @e2e",
    "test:security": "playwright test --grep @security",
    "test:permission": "playwright test --grep @permission",
    "test:followup": "playwright test --grep @followup",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "auth:generate": "tsx scripts/generate-auth-state.ts",
    "health": "tsx scripts/health-check.ts",
    "data:create": "tsx scripts/create-test-data.ts",
    "data:cleanup": "tsx scripts/cleanup-test-data.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.10.0",
    "dotenv": "^16.4.7",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
