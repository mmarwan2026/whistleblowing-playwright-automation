import { Page } from '@playwright/test';

export class InvestigationFlow {
  constructor(private readonly page: Page) {}

  async run() {
    // TODO: compose confirmed page actions for the real Investigation workflow.
  }
}
