import { Page } from '@playwright/test';

export class AapaFlow {
  constructor(private readonly page: Page) {}

  async run() {
    // TODO: compose confirmed page actions for the real Aapa workflow.
  }
}
