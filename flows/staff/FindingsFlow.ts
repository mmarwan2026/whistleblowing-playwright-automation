import { Page } from '@playwright/test';

export class FindingsFlow {
  constructor(private readonly page: Page) {}

  async run() {
    // TODO: compose confirmed page actions for the real Findings workflow.
  }
}
