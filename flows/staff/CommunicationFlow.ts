import { Page } from '@playwright/test';

export class CommunicationFlow {
  constructor(private readonly page: Page) {}

  async run() {
    // TODO: compose confirmed page actions for the real Communication workflow.
  }
}
